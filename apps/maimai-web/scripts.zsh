# For Later Uses

# Downloading Rating Image
x=green ; for i in $(seq 0 9); do wget https://chunithm-net-eng.com/mobile/images/rating/rating_${x}_0${i}.png -O $i.png; done ; wget https://chunithm-net-eng.com/mobile/images/rating/rating_${x}_comma.png -O comma.png
