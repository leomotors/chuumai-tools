{
  "name": "@repo/utils-chuni",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint src && tsc",
    "format": "eslint src --fix",
    "test": "vitest run"
  },
  "main": "./src/index.ts",
  "devDependencies": {
    "vitest": "^3.1.1"
  }
}
