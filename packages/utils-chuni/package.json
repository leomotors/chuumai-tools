{
  "name": "@repo/utils-chuni",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "lint": "eslint src && tsc",
    "format": "eslint src --fix",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.1.1"
  }
}
